generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}
datasource db {
  provider = "postgresql"
}
enum RoleCode {
  ADMIN
  DEVELOPER
  STUDENT
  DEAN_OFFICE
  CURATOR
  TEACHER
  STAROSTA
}
enum StatusCode {
  PRESENT
  N
  HV
}
enum JournalState {
  EMPTY
  DRAFT
  CONFIRMED
}
enum ReportState {
  PENDING
  READY
  FAILED
}
model Role {
  id    RoleCode @id
  label String
  users UserRole[]
}
model User {
  id           String @id @default(cuid())
  email        String @unique
  name         String
  passwordHash String
  active       Boolean @default(true)
  mustChangePassword Boolean @default(true)
  position String @default("UNSPECIFIED")
  workspace RoleCode?
  passwordChangedAt DateTime? @db.Timestamptz(3)
  createdAt    DateTime @default(now()) @db.Timestamptz(3)
  roles        UserRole[]
  sessions     Session[]
  teacher      Teacher?
  student      Student?
  curatorAssignments CuratorAssignment[]
  starostaAssignments StarostaAssignment[]
  deanAssignments DeanAssignment[]
  auditLogs    AuditLog[]
  submissions  JournalSubmission[]
  reports      Report[]
  feedback Feedback[]
}
model UserRole {
  userId String
  roleId RoleCode
  user User @relation(fields: [userId], references: [id], onDelete: Restrict)
  role Role @relation(fields: [roleId], references: [id], onDelete: Restrict)
  @@id([userId, roleId])
}
model Session {
  id String @id @default(cuid())
  tokenHash String @unique
  userId String
  expiresAt DateTime @db.Timestamptz(3)
  createdAt DateTime @default(now()) @db.Timestamptz(3)
  user User @relation(fields: [userId], references: [id], onDelete: Restrict)
  lastSeenAt DateTime @default(now()) @db.Timestamptz(3)
  device String @default("Невідомий пристрій")
  @@index([userId, expiresAt])
}
model LoginBucket {
  key String @id
  attempts Int @default(0)
  resetAt DateTime @db.Timestamptz(3)
  strikes Int @default(0)
  blockedUntil DateTime? @db.Timestamptz(3)
}
model Faculty {
  id String @id @default(cuid())
  slug String @unique
  name String
  sourceUrl String?
  specialties Specialty[]
  deanAssignments DeanAssignment[]
  reports Report[]
  terms AcademicTerm[]
}
model Specialty {
  id String @id
  name String
  code String?
  codeNote String?
  programName String?
  facultyId String
  source Json
  faculty Faculty @relation(fields: [facultyId], references: [id], onDelete: Restrict)
  groups Group[]
  @@index([facultyId])
}
model Group {
  id String @id
  name String @unique
  course Int
  specialtyId String
  intakeYear Int?
  source Json
  specialty Specialty @relation(fields: [specialtyId], references: [id], onDelete: Restrict)
  students Student[]
  lessons LessonGroup[]
  curatorAssignments CuratorAssignment[]
  starostaAssignments StarostaAssignment[]
  roster LessonStudent[]
  teachingAssignments TeacherGroup[]
  @@index([specialtyId, course])
}
model Student {
  id String @id
  fullName String
  phone String?
  isSynthetic Boolean @default(true)
  active Boolean @default(true)
  archivedAt DateTime? @db.Timestamptz(3)
  createdAt DateTime @default(now()) @db.Timestamptz(3)
  joinedAt DateTime @default(now()) @db.Timestamptz(3)
  source Json?
  importKey String? @unique
  groupId String
  userId String? @unique
  group Group @relation(fields: [groupId], references: [id], onDelete: Restrict)
  user User? @relation(fields: [userId], references: [id], onDelete: Restrict)
  attendance Attendance[]
  roster LessonStudent[]
  sourceRows RosterSourceRow[]
  @@index([groupId, fullName])
}
model Teacher {
  id String @id
  displayName String
  userId String? @unique
  source Json
  retiredAt DateTime? @db.Timestamptz(3)
  position String @default("UNSPECIFIED")
  user User? @relation(fields: [userId], references: [id], onDelete: Restrict)
  lessons Lesson[]
  subjects TeacherSubject[]
  groups TeacherGroup[]
}
model CuratorAssignment {
  userId String
  groupId String
  user User @relation(fields: [userId], references: [id], onDelete: Restrict)
  group Group @relation(fields: [groupId], references: [id], onDelete: Restrict)
  @@id([userId, groupId])
}
model StarostaAssignment {
  userId String
  groupId String
  user User @relation(fields: [userId], references: [id], onDelete: Restrict)
  group Group @relation(fields: [groupId], references: [id], onDelete: Restrict)
  @@id([userId, groupId])
}
model SystemState {
  id String @id
  value Json
  updatedAt DateTime @updatedAt @db.Timestamptz(3)
}
model DeanAssignment {
  userId String
  facultyId String
  user User @relation(fields: [userId], references: [id], onDelete: Restrict)
  faculty Faculty @relation(fields: [facultyId], references: [id], onDelete: Restrict)
  @@id([userId, facultyId])
}
model Subject {
  id String @id
  name String
  source Json
  lessons Lesson[]
  teachers TeacherSubject[]
}
model Building {
  id String @id
  abbreviation String @unique
  name String?
  address String?
  confirmed Boolean @default(false)
  source Json
  lessons Lesson[]
  bells Bell[]
}
model Bell {
  id String @id
  pairNumber Int
  startTime String
  endTime String
  buildingId String?
  source String
  building Building? @relation(fields: [buildingId], references: [id], onDelete: Restrict)
  lessons Lesson[]
}
model SourceRecord {
  id String @id
  file String
  page Int
  raw String
  bbox Json
  data Json
  issues Json
  lessons Lesson[]
  @@index([file, page])
}
model Lesson {
  id String @id
  startAt DateTime @db.Timestamptz(3)
  endAt DateTime @db.Timestamptz(3)
  pairNumber Int
  subjectId String
  teacherId String?
  buildingId String
  room String
  onlineUrl String?
  termId String?
  term AcademicTerm? @relation(fields: [termId], references: [id], onDelete: Restrict)
  bellId String
  sourceId String?
  synthetic Boolean @default(true)
  cancelled Boolean @default(false)
  starostaAllowed Boolean @default(true)
  kind String @default("Демонстраційне заняття")
  subgroup String?
  weekPattern String?
  journalState JournalState @default(EMPTY)
  version Int @default(0)
  updatedAt DateTime @updatedAt @db.Timestamptz(3)
  subject Subject @relation(fields: [subjectId], references: [id], onDelete: Restrict)
  teacher Teacher? @relation(fields: [teacherId], references: [id], onDelete: Restrict)
  building Building @relation(fields: [buildingId], references: [id], onDelete: Restrict)
  bell Bell @relation(fields: [bellId], references: [id], onDelete: Restrict)
  source SourceRecord? @relation(fields: [sourceId], references: [id], onDelete: Restrict)
  groups LessonGroup[]
  roster LessonStudent[]
  attendance Attendance[]
  auditLogs AuditLog[]
  submissions JournalSubmission[]
  @@index([teacherId, startAt])
  @@index([termId, startAt])
  @@index([startAt, cancelled])
  @@index([buildingId, room, startAt])
}
model LessonGroup {
  lessonId String
  groupId String
  lesson Lesson @relation(fields: [lessonId], references: [id], onDelete: Restrict)
  group Group @relation(fields: [groupId], references: [id], onDelete: Restrict)
  @@id([lessonId, groupId])
  @@index([groupId, lessonId])
}
model LessonStudent {
  lessonId String
  studentId String
  // Snapshot of membership at this lesson, preserved across transfers.
  groupId String?
  group Group? @relation(fields: [groupId], references: [id], onDelete: Restrict)
  lesson Lesson @relation(fields: [lessonId], references: [id], onDelete: Restrict)
  student Student @relation(fields: [studentId], references: [id], onDelete: Restrict)
  attendance Attendance?
  @@id([lessonId, studentId])
  @@index([studentId])
  @@index([groupId, lessonId])
}
model AttendanceStatus {
  code StatusCode @id
  label String
  records Attendance[]
}
model Attendance {
  studentId String
  lessonId String
  statusCode StatusCode
  confirmed Boolean @default(false)
  isDemo Boolean @default(false)
  source String @default("MANUAL")
  batchId String?
  createdAt DateTime @default(now()) @db.Timestamptz(3)
  batch BackfillBatch? @relation(fields: [batchId], references: [id], onDelete: Restrict)
  updatedAt DateTime @updatedAt @db.Timestamptz(3)
  student Student @relation(fields: [studentId], references: [id], onDelete: Restrict)
  lesson Lesson @relation(fields: [lessonId], references: [id], onDelete: Restrict)
  roster LessonStudent @relation(fields: [lessonId, studentId], references: [lessonId, studentId], onDelete: Restrict)
  status AttendanceStatus @relation(fields: [statusCode], references: [code], onDelete: Restrict)
  @@id([studentId, lessonId])
  @@unique([lessonId, studentId])
  @@index([lessonId, statusCode, confirmed])
}
model JournalSubmission {
  id String @id
  lessonId String
  userId String
  payloadHash String
  resultVersion Int
  createdAt DateTime @default(now()) @db.Timestamptz(3)
  lesson Lesson @relation(fields: [lessonId], references: [id], onDelete: Restrict)
  user User @relation(fields: [userId], references: [id], onDelete: Restrict)
}
model AuditLog {
  id String @id @default(cuid())
  actorId String?
  lessonId String?
  studentId String?
  groupId String?
  objectType String
  objectId String
  oldStatus StatusCode?
  newStatus StatusCode?
  reason String?
  source String
  details Json?
  createdAt DateTime @default(now()) @db.Timestamptz(3)
  actor User? @relation(fields: [actorId], references: [id], onDelete: Restrict)
  lesson Lesson? @relation(fields: [lessonId], references: [id], onDelete: Restrict)
  @@index([lessonId, createdAt])
  @@index([objectType, objectId, createdAt])
  @@index([createdAt])
  @@index([actorId, createdAt])
}
model Report {
  id String @id @default(cuid())
  key String @unique
  facultyId String
  createdById String?
  kind String
  fromDate String
  toDate String
  filters Json
  state ReportState @default(PENDING)
  summary Json?
  csv Bytes?
  pdf Bytes?
  xlsx Bytes?
  error String?
  alertKey String? @unique
  createdAt DateTime @default(now()) @db.Timestamptz(3)
  updatedAt DateTime @updatedAt @db.Timestamptz(3)
  faculty Faculty @relation(fields: [facultyId], references: [id], onDelete: Restrict)
  createdBy User? @relation(fields: [createdById], references: [id], onDelete: Restrict)
  @@index([facultyId, fromDate, toDate])
}

model AcademicTerm {
  id String @id
  name String
  facultyId String
  fromDate String
  toDate String
  confirmedAt DateTime? @db.Timestamptz(3)
  archivedAt DateTime? @db.Timestamptz(3)
  rosterConfirmedAt DateTime? @db.Timestamptz(3)
  createdAt DateTime @default(now()) @db.Timestamptz(3)
  faculty Faculty @relation(fields: [facultyId], references: [id], onDelete: Restrict)
  lessons Lesson[]
  @@index([facultyId, fromDate, toDate])
}

model TeacherSubject {
  teacherId String
  subjectId String
  teacher Teacher @relation(fields: [teacherId], references: [id], onDelete: Restrict)
  subject Subject @relation(fields: [subjectId], references: [id], onDelete: Restrict)
  @@id([teacherId, subjectId])
}

model TeacherGroup {
  teacherId String
  groupId String
  teacher Teacher @relation(fields: [teacherId], references: [id], onDelete: Restrict)
  group Group @relation(fields: [groupId], references: [id], onDelete: Restrict)
  @@id([teacherId, groupId])
}

model SourceAsset {
  id String @id
  fileName String
  mimeType String
  sha256 String @unique
  content Bytes
  groupIds Json
  createdAt DateTime @default(now()) @db.Timestamptz(3)
  rows RosterSourceRow[]
}

model RosterSourceRow {
  sourceId String
  row Int
  groupId String
  fullName String
  studentId String
  provenance Json
  source SourceAsset @relation(fields: [sourceId], references: [id], onDelete: Restrict)
  student Student @relation(fields: [studentId], references: [id], onDelete: Restrict)
  @@id([sourceId, row])
  @@unique([sourceId, studentId])
  @@index([studentId])
}

model BackfillBatch {
  id String @id
  algorithm String
  seed String
  fromDate String
  toDate String
  sourceId String
  parameters Json
  result Json
  createdAt DateTime @default(now()) @db.Timestamptz(3)
  attendance Attendance[]
}

model Feedback {
  id String @id @default(cuid())
  userId String
  roles Json
  workspace String?
  page String
  text String
  state String @default("NEW")
  createdAt DateTime @default(now()) @db.Timestamptz(3)
  updatedAt DateTime @updatedAt @db.Timestamptz(3)
  user User @relation(fields: [userId], references: [id], onDelete: Restrict)
  @@index([state, createdAt])
  @@index([createdAt])
}
