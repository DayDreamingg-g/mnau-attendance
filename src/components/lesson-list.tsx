import {filterLink,type Filters} from '@/lib/filters';
import Link from 'next/link';
import {journalStateForRoster,journalStateLabel} from '@/lib/journal-state';
import {dateLabel,timeLabel} from '@/lib/time';
import {Empty} from './ui';
type Lesson={id:string;startAt:Date;endAt:Date;pairNumber:number;room:string;cancelled:boolean;journalState:string;subject:{name:string};building:{abbreviation:string};groups:{group:{name:string}}[];attendance:{statusCode:string;confirmed:boolean}[];_count:{roster:number}};
export function LessonList({lessons,filters}:{lessons:Lesson[];filters?:Filters}){return lessons.length?<div className="lesson-grid">{lessons.map(l=>{const counts={PRESENT:0,N:0,HV:0};for(const a of l.attendance)if(a.confirmed)counts[a.statusCode as keyof typeof counts]++;const state=journalStateForRoster(l._count.roster,l.attendance);const pending=l.attendance.filter(a=>!a.confirmed).length;return <Link key={l.id} className="lesson-card" href={filters?filterLink(`/teacher/lessons/${l.id}`,filters):`/teacher/lessons/${l.id}`}><div className="lesson-meta"><span>{dateLabel(l.startAt)} · {l.pairNumber} пара</span><span>{timeLabel(l.startAt)}–{timeLabel(l.endAt)}</span></div><h2>{l.subject.name}</h2><div className="lesson-meta"><span>{l.groups.map(g=>g.group.name).join(' + ')}</span><span>{l.building.abbreviation} {l.room}</span></div><div><span className="tag">{l.cancelled?'Скасовано':journalStateLabel(state)}</span></div><div className="lesson-counts"><span className="success">• {counts.PRESENT}</span><span className="danger">N {counts.N}</span><span className="info">HV {counts.HV}</span><span className="muted">Чернетки: {pending}</span><span className="muted">Не відмічено: {l._count.roster-l.attendance.length}</span><span className="text-link" style={{marginLeft:'auto'}}>Відкрити →</span></div></Link>;})}</div>:<PanelEmpty/>;}
function PanelEmpty(){return <div className="panel section-space"><Empty>За обраною датою або пошуком занять немає.</Empty></div>;}
