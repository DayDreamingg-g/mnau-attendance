'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
export function ActionForm({url,values,label,children,confirm=false}:{url:string;values:Record<string,unknown>;label:string;children?:React.ReactNode;confirm?:boolean}){const router=useRouter(),[busy,setBusy]=useState(false),[message,setMessage]=useState(''),[failed,setFailed]=useState(false);return <form className="filters" onSubmit={async e=>{e.preventDefault();setBusy(true);setMessage('');setFailed(false);try{const fields=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...values,...fields,...(confirm?{confirm:fields.confirm==='on'}:{})})});const data=await r.json();if(!r.ok)throw new Error(data.error);setMessage('Збережено.');router.refresh();}catch(e){setFailed(true);setMessage(e instanceof Error?e.message:'Помилка');}finally{setBusy(false);}}}>{children}{confirm&&<label><input type="checkbox" name="confirm" required/>Підтверджую дію</label>}<button className="button" disabled={busy}>{busy?'Збереження…':label}</button>{message&&<p role={failed?'alert':'status'} className={failed?'error-text':'form-success'}>{message}</p>}</form>;}
