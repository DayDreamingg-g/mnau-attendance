'use client';
import {roleLabels} from '@/lib/role-labels';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
export function SelfRoles({roles}:{roles:string[]}){const router=useRouter(),[busy,setBusy]=useState(false),[error,setError]=useState('');return <details className="source-record"><summary>Змінити власні ролі розробника</summary><form className="filters" onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);setBusy(true);try{const r=await fetch('/api/admin/release',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'SELF_ROLES',roles:f.getAll('roles'),confirm:f.get('confirm')==='on',reason:f.get('reason')})});const result=await r.json();if(!r.ok)throw new Error(result.error);router.push('/');router.refresh();}catch(e){setError(e instanceof Error?e.message:'Помилка');}finally{setBusy(false);}}}><label>Ролі<select name="roles" multiple defaultValue={roles}>{['DEVELOPER','ADMIN','TEACHER','CURATOR','STAROSTA','DEAN_OFFICE'].map(r=><option key={r} value={r}>{roleLabels[r]}</option>)}</select></label><label>Причина<input name="reason" minLength={5} maxLength={500} required/></label><label><input type="checkbox" name="confirm" required/>Підтверджую зміну власних прав</label><button className="button" disabled={busy}>Зберегти ролі</button>{error&&<p role="alert">{error}</p>}</form></details>;}
