import {requireUser} from '@/lib/auth';
import {db} from '@/lib/db';
import {PageTitle,Panel} from '@/components/ui';
import type {Search} from '@/lib/filters';
export default async function Sources({searchParams}:{searchParams:Promise<Search>}){await requireUser();const p=await searchParams;const q=typeof p.q==='string'?p.q.slice(0,100):'';const records=await db.sourceRecord.findMany({where:q?{raw:{contains:q,mode:'insensitive'}}:undefined,orderBy:[{file:'asc'},{id:'asc'}]});return <><PageTitle eyebrow="ПОХОДЖЕННЯ ДАНИХ ДАНИХ" title="Джерела та уточнення" description="Клітинки PDF з координатами та вихідним записом. Це довідник джерел, а не підтверджений календар занять."/><Panel title="Що підтверджено"><div className="pad stack"><p>28 груп факультету менеджменту: 5 на 1 курсі, 7 на 2 курсі, 9 на 3 курсі, 7 на 4 курсі. Кожна група має 15 вигаданих студентів.</p><p>Економіка 1 курсу: заголовок є, позначення групи відсутнє. Групу не створено. Психологію, облік і фінанси не включено до менеджменту.</p><p>Поділ клітинок збережено без припущення про чисельник/знаменник тижня. «Вакансія» не створює викладача. «кр» потребує уточнення, «карп» має окремий запис.</p><p>Коди наборів і назви освітніх програм, не зазначені у PDF, залишено незаповненими. Довідкові коди з офіційних сторінок не присвоєно автоматично новим наборам.</p><p><a className="text-link" href="https://www.mnau.edu.ua/faculty-men/" target="_blank" rel="noreferrer">Факультет менеджменту ↗</a> · <a className="text-link" href="https://www.mnau.edu.ua/faculty-off/" target="_blank" rel="noreferrer">Обліково-фінансовий факультет ↗</a></p></div></Panel><form className="filters section-space"><label>Пошук у джерелах<input name="q" defaultValue={q} placeholder="Прізвище, дисципліна…"/></label><button className="button primary">Знайти</button></form><Panel title={`Вихідні клітинки · ${records.length}`}>{records.map(r=><details className="source-record" key={r.id}><summary>{r.file} · стор. {r.page} · {r.raw.split('\n')[0]}{Array.isArray(r.issues)&&r.issues.length?' · Потребує уточнення':''}</summary><pre>{r.raw}</pre><p className="small muted">Координати: {JSON.stringify(r.bbox)}</p>{Array.isArray(r.issues)&&r.issues.map((x,i)=><p className="small warning" key={i}>{String(x)}</p>)}</details>)}</Panel></>;}
